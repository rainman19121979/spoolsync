# Quickstart (Linux, ohne Docker)
```bash
sudo apt update
sudo apt install -y git python3 python3-venv python3-pip
git clone https://github.com/DEINUSER/SpoolSync.git
cd SpoolSync
sudo ./scripts/install-spoolsync.sh
```
